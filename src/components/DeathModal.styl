.modal-overlay {
  width: 100%;
  height: 100%;
  z-index: 50;
  position: absolute;
  top: 0;
  right: 0;
  background: rgba(0, 0, 0, 0.3);

  .modal {
    width: 300px;
    height: 300px;
    margin: auto;
    padding: 20px;
    background: white;
  }
}